name = "Reflection Demo"
version = "0.1.0"
author = "RML"
description = "Exercises the classes registered by the native part of the mod."

[datamodel.context]
standalone = ["modthing.luau"]
edit = ["modthing.luau"]
